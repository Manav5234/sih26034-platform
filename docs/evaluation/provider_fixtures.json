{
  "version": 1,
  "notes": "Optional deterministic replacement for external ProductLookupAdapter responses. Add a barcode key only when a regression case intentionally needs provider evidence.",
  "barcodes": {}
}
